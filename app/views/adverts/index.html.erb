<%= link_to "Déposer une annonce", new_advert_path, class: "btn btn-primary"  %>

<% @adverts.each do |advert| %>
  <%= link_to advert_path(advert) do  %>
    <%= advert.title %>
    <%= advert.price %>
  <% end %>
<% end %>
